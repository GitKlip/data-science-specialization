# plotly & ggplot2
# Plotly is a platform for creating interactive graphics and dealing analysis. However, we're going to focus on Plotly as it interacts with R. 
# SImilar to what we're getting out of R charts in googleVis. The difference is Plotly will interact with a specific web platform. 


install.packages("devtools")
require(devtools)
install_github("ropensci/plotly")
library(plotly)